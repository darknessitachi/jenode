package ENode.Eventing;

import ENode.Messaging.*;

/** Represents an executor to execute an uncommitted event stream message.
 
*/
public interface IUncommittedEventExecutor extends IMessageExecutor<EventStream>
{
}